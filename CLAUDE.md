@AGENTS.md

<!-- An import, not a link: Claude Code expands `@AGENTS.md` into context at
     session start, where a prose "see AGENTS.md" only asks it to go and read
     one. It reads CLAUDE.md and not AGENTS.md, so this file is the bridge.
     https://code.claude.com/docs/en/memory#agents-md

     Nothing Claude-specific belongs below the import yet. If something does,
     it goes here rather than in AGENTS.md, which every other agent reads. -->
